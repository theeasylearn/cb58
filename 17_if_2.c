/*
    write a program to findout what type of shape (landscape,portrait,square) from given length & width
    step 
    1) create variable length & width
    2) accept input for length & width
    3) if length equals to width then print message shape is square 
    4) if length is more then width then print message shape is portrait 
    5) if length is less then width then print message shape is landscape 
*/
